# Unpack Tool 使用说明

Unpack Tool 用于批量下载 `.torrent` 文件，并将种子推送到 qBittorrent 或 Transmission。程序会保存任务、下载结果和设置，电脑重启后可以继续下载，也可以直接推送之前已经下载完成的种子。

## 1. 安装与启动

1. 从 GitHub Releases 下载最新的 `unpack_tool-vX.Y.Z-windows-x64.zip`。
2. 将压缩包完整解压到一个可长期保留的目录，不要只把 EXE 单独拖出运行。
3. 双击 `unpack_tool.exe`。

程序数据保存在 EXE 所在目录：

- `links`：链接模板文件。
- `torrents`：已下载的本地种子文件。
- `app_data.db`：任务状态和软件设置。

请保留 `torrents` 和 `app_data.db`，否则无法恢复之前的任务状态。升级时可以把新版文件解压到原目录，并保留这两个数据项。

## 2. 准备 links 文件

程序读取 `links` 文件夹中的 `.txt` 文件，每行一条记录，支持以下两种格式。

普通链接，种子会推送到统一的做种路径：

```text
{website}/download.php?id=100&passkey={passkey}
```

指定子路径，竖线左侧是相对于做种路径的子目录：

```text
电影合集/第一部|{website}/download.php?id=100&passkey={passkey}
```

生成任务时，程序会用界面中的 Website 和 Passkey 替换 `{website}` 与 `{passkey}`。仍兼容旧模板中的 `https://website` 和 `yourpasskey`。

注意事项：

- 文件使用 UTF-8 编码。
- 空行和以 `#` 开头的行会被忽略。
- 子路径必须是相对路径，不能使用盘符、根路径或 `..`。
- 链接文件可能包含私人 passkey，公开分享前请先替换为 `{passkey}` 占位符。

## 3. 配置任务

在“任务”页面完成以下配置：

1. 选择链接文件；新增文件后点击“刷新”。
2. 填写 Website，例如 `https://tracker.example.com`。
3. 填写 Passkey。
4. 填写做种路径。
5. 设置随机下载间隔的最小值和最大值，单位为秒。
6. 点击“保存设置”。

做种路径是下载器所在设备能够访问的内容目录，不是种子文件的保存目录。例如下载器运行在 NAS 上，应填写 NAS 中 qBittorrent 或 Transmission 看到的路径。使用指定子路径的链接时，界面底部会显示最终推送路径预览。

## 4. 配置下载器

打开“下载器与设置”页面，选择下载器类型并填写连接信息，然后点击“测试连接”。

### qBittorrent

- 地址：qBittorrent Web UI 所在设备的 IP 或域名。
- 端口：Web UI 端口，常见值为 `8080`。
- 用户名和密码：qBittorrent Web UI 登录信息。
- qBittorrent 4.x 和 5.x 均使用用户名、密码及 Cookie 会话登录，不使用 API Key。

请确保 qBittorrent 已启用 Web UI，并允许当前电脑访问。

### Transmission

- 地址：Transmission 所在设备的 IP 或域名。
- 端口：RPC 端口，常见值为 `9091`。
- 用户名和密码：启用认证时填写，未启用时可以留空。

程序会自动处理 Transmission RPC 的 Session ID 更新。

## 5. 下载与推送

1. 在“任务”页面点击“生成任务”。生成新任务会替换当前任务列表。
2. 点击“开始下载”。程序按随机间隔逐个下载种子，并在每个种子完成后保存状态。
3. 需要暂时停止下载时，点击“暂停下载”；再次点击可继续。
4. 下载完成并测试下载器连接后，点击“开始推送”。

推送选项：

- `添加后暂停`：种子仍会正常添加到下载器，但在 qBittorrent 或 Transmission 中保持暂停状态。
- `推送后删除本地种子`：成功推送后删除 `torrents` 中的本地种子。默认不勾选，建议保留默认设置。
- `选中项重新推送`：将列表中选中的已下载记录改为待推送状态，便于再次添加。

## 6. 中断与状态恢复

程序关闭、电脑关机或异常中断后，再次启动会读取 `app_data.db`，并扫描 `torrents` 文件夹：

- 未下载的任务可以继续下载。
- 已下载但未推送的任务可以直接点击“开始推送”。
- 已推送记录会保留状态，不会自动重复推送。
- 随机下载间隔、下载器连接、路径和推送选项会自动恢复。

运行任务时直接关闭程序，当前请求可能尚未完成；重新打开后，从列表中剩余项目继续即可。

## 7. 无种子时测试

没有可用种子时，可以测试以下内容：

- 点击“测试连接”，确认下载器版本和连接状态。
- 选择带子路径的 links 文件并生成任务，选中列表项查看底部目标路径预览。
- 要验证真实推送，必须准备一个合法的 `.torrent` 文件。程序不会生成虚假测试种子，也不会用无效数据污染下载器。

已有合法种子但列表中没有记录时，将文件放入程序旁的 `torrents` 文件夹并重启。程序会识别本地种子并将其加入待推送列表；推送前请确认做种路径正确。

## 8. 常见问题

### 点击 EXE 没有反应

- 使用发布压缩包中的 `unpack_tool.exe`，不要运行 `build` 目录中的 PyInstaller 中间文件。
- 完整解压 ZIP 后再运行，避免直接在压缩包预览中启动。
- 确认程序目录可写，因为软件需要创建数据库和 `torrents` 文件夹。

### qBittorrent 连接失败

- 在浏览器中确认相同地址和端口能打开 Web UI。
- 检查用户名、密码、防火墙和 Web UI 访问限制。
- 地址可以填写 `127.0.0.1`、局域网 IP 或完整的 `http://` / `https://` 地址。

### 连接成功但推送失败

- 确认做种路径是下载器所在设备看到的路径。
- NAS、Docker 或远程下载器需要填写容器或远程系统内部可访问的路径。
- 查看“运行日志”中的下载器返回信息。
- 如果下载器中已经存在同一个种子，Transmission 会按重复种子正常返回；qBittorrent 的行为取决于其 Web UI 设置。

### 重启后无法继续

- 确认 `app_data.db` 和 `torrents` 仍在 EXE 所在目录。
- 不要在升级或移动程序时只复制 EXE。
- 若本地种子存在但状态异常，选中记录后点击“选中项重新推送”。
